# Optional. Copy to .env for local dev (e.g. with `vercel dev`).
# On Vercel, set these in: Project → Settings → Environment Variables.

# Rate limit: max requests per IP per window (default: 100)
# RATE_LIMIT_MAX=100

# Rate limit window in milliseconds (default: 900000 = 15 min)
# RATE_LIMIT_WINDOW_MS=900000

# Max length of QR text in characters (default: 2000)
# MAX_TEXT_LENGTH=2000

# Optional API key. If set, /qr requires X-API-Key header or ?apiKey=...
# Health and GET / remain public.
# API_KEY=your-secret-key
